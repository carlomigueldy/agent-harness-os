# {{PROJECT_NAME}} — Agent Harness OS

<!-- FILL: Replace the placeholder above with your actual project name. -->

> TEMPLATE NOTE: This file is the entry point for all agents and human developers. Keep it concise (50–200 lines). Move detail into `.agents/context/*` or `.agents/workflows/*`.
>
> **First time adopting this template?** Do the one-time [adoption workflow](.agents/workflows/adoption.md) first — fill the `{{placeholders}}` and `<!-- FILL -->` markers — then use the reading order below.

## Project Overview

<!-- FILL: 2–4 sentences describing what this project is, what it does, and who uses it. -->

- **Type:** {{PROJECT_TYPE}}
- **Language:** {{PRIMARY_LANGUAGE}}
- **Package manager:** {{PACKAGE_MANAGER}}
- **Deployment:** {{DEPLOYMENT_TARGET}}
- **Repo:** {{GITHUB_OWNER}}/{{REPO_NAME}} — default branch `{{DEFAULT_BRANCH}}`

## Quick Start

```bash
# 1. Run baseline setup
bash init.sh

# 2. Install dependencies
{{INSTALL_CMD}}

# 3. Start dev server
{{DEV_CMD}}
```

See [commands](.agents/context/commands.md) for the full command reference.

First time? Provision placeholders: `bash scripts/provision.sh --help` (or `--dry-run` to preview). See [adoption workflow](.agents/workflows/adoption.md).

## Required Reading Order

New session or new agent? Read in this order before doing anything else:

1. This file (`AGENTS.md`)
2. [`CLAUDE.md`](CLAUDE.md) — Claude Code workflow optimizations
3. [`claude-progress.md`](claude-progress.md) — current state snapshot
4. [`.agents/logs/progress.md`](.agents/logs/progress.md) — detailed progress log
5. [`session-handoff.md`](session-handoff.md) — last handover
6. [`.agents/workflows/initialization.md`](.agents/workflows/initialization.md) — full session start checklist
7. [`feature_list.json`](feature_list.json) — machine-readable feature status

## The Five Harness Subsystems

| Subsystem | Purpose | Key Files |
|---|---|---|
| **Instructions** | Rules, constraints, definitions of done | `AGENTS.md`, `CLAUDE.md`, `.agents/context/` |
| **Tools** | Slash commands, skills, scripts, MCP tools | `.claude/commands/`, `.claude/skills/`, `.agents/context/slash-commands.md`, `.agents/context/skills.md` |
| **Environment** | Runtimes, env vars, local setup | `.agents/context/environment.md`, `init.sh` |
| **State** | Progress, decisions, handovers, feature list | `claude-progress.md`, `.agents/logs/`, `feature_list.json` |
| **Feedback** | Review loops, verification, rubric, retrospectives | `evaluator-rubric.md`, `.agents/logs/verification.md`, `.agents/logs/retrospectives.md` |

## Hard Constraints

These are non-negotiable in every session:

- **No LLM/AI attribution.** Never write "Generated by", "Claude", "Co-authored-by: Claude", "AI-assisted", or any equivalent — in commits, PRs, changelogs, comments, or docs.
- **Never commit secrets.** Check `.env`, API keys, tokens, and private keys before every push. See [environment](.agents/context/environment.md).
- **Do not fake evidence.** If verification cannot run, document what was attempted, why it failed, and what risk remains.
- **Do not force parallelism.** Use parallel work only when workstreams are genuinely independent. When uncertain, work sequentially.
- **Prefer skills when applicable.** Check [skills](.agents/context/skills.md) before implementing repeated patterns manually.

## Verification Commands

<!-- FILL: Confirm these commands work in your project before using the harness. -->

```bash
{{LINT_CMD}}       # lint
{{TYPECHECK_CMD}}  # type check
{{TEST_CMD}}       # unit / integration tests
{{BUILD_CMD}}      # build
{{E2E_CMD}}        # end-to-end tests (if applicable)
{{FORMAT_CMD}}     # format
```

Full reference: [`.agents/context/commands.md`](.agents/context/commands.md)

## Definition of Done

A task is done only when **all** of the following are true:

- [ ] Implementation is complete and scoped to the task
- [ ] Verification ran (lint, typecheck, tests, build) — or failure is documented
- [ ] Evidence exists (test output, screenshot, CLI output, or justified explanation)
- [ ] No secrets are staged or committed
- [ ] No LLM/AI attribution anywhere
- [ ] `feature_list.json` updated
- [ ] `claude-progress.md` and `.agents/logs/progress.md` updated
- [ ] `.agents/logs/changelog.md` updated
- [ ] Handover written in `session-handoff.md` and `.agents/logs/handover.md`
- [ ] GitHub issue/PR updated (if applicable)
- [ ] Worktree is clean or dirty state is explained

## Progress File Locations

| File | Purpose |
|---|---|
| [`claude-progress.md`](claude-progress.md) | Session-level snapshot |
| [`.agents/logs/progress.md`](.agents/logs/progress.md) | Detailed running log with sprint contracts |
| [`feature_list.json`](feature_list.json) | Machine-readable feature status |
| [`.agents/logs/decisions.md`](.agents/logs/decisions.md) | Architectural and workflow decisions |

## Handover Protocol

At the end of every significant session, write a compact handover.

- Root file: [`session-handoff.md`](session-handoff.md)
- Log file: [`.agents/logs/handover.md`](.agents/logs/handover.md)
- Full format: [`.agents/workflows/handover.md`](.agents/workflows/handover.md)

## GitHub Issue Workflow

When GitHub is available, use Issues as the project progress system.

Full workflow: [`.agents/workflows/github-issues.md`](.agents/workflows/github-issues.md)

Issue templates: `.github/ISSUE_TEMPLATE/`
Epic-branch delivery + ledger sync: [`.agents/workflows/epic-delivery.md`](.agents/workflows/epic-delivery.md)

## Worktree Session Workflow

Every meaningful coding session should use a dedicated git worktree.

- Branch naming: `feat/`, `fix/`, `docs/`, `chore/`, `refactor/`, `test/`, `harness/`
- Worktree location: `.agents/worktrees/<branch-name>` (in-repo, gitignored; never outside the project dir)
- Full workflow: [`.agents/workflows/worktree-sessions.md`](.agents/workflows/worktree-sessions.md)
- Worktree context: [`.agents/context/worktrees.md`](.agents/context/worktrees.md)
- Helper: `bash scripts/worktree.sh create <branch>` — validates prefix, creates at `.agents/worktrees/<branch>` (default, gitignored), copies env files; `--sibling` is the escape hatch

## Orchestration Mode Selection

Before starting a non-trivial task, choose the right execution mode:

| Situation | Mode |
|---|---|
| Small or medium task | Single agent |
| Focused investigation | Subagent |
| Repeatable audit / migration | Dynamic workflow |
| Complex collaborative feature | Agent team |
| Shared files, migrations, high-risk | Sequential single agent |
| Frontend / visual / design work | Single agent + `/frontend-design` |

Full matrix and rules: [`.agents/workflows/orchestration.md`](.agents/workflows/orchestration.md)

Default hard limits: 3 subagents, 4-agent teams, 8 dynamic-workflow workers, 6 feedback iterations.

## Skill and Superpower Usage

- Check [`.agents/context/skills.md`](.agents/context/skills.md) for available skills before implementing patterns manually.
- Use `/frontend-design` for UI, UX, layout, accessibility, and screenshot-driven work when available.
- Use `/superpowers:*` skills when they apply to the task.
- Do not use skills for the sake of using skills — use them when they improve quality, speed, or consistency.
- Propose new skills in `.agents/proposals/` when repeated patterns appear.

## Slash Commands, Skills, Subagents & Loops

This harness ships **invokable** Claude Code surfaces, not just docs:

- **Slash commands** ([`.claude/commands/`](.claude/commands/)) — entry points like `/gated-orchestration`, `/review-10x`, `/create-skill`, `/final-handoff`. Index + schema: [`.agents/context/slash-commands.md`](.agents/context/slash-commands.md).
- **Skills** ([`.claude/skills/`](.claude/skills/)) — reusable procedures (`opus-code-review`, `repo-discovery`, `handoff-writing`, …) loaded on demand. Index + schema: [`.agents/context/skills.md`](.agents/context/skills.md).
- **Subagents** ([`.claude/agents/`](.claude/agents/)) — reusable, model-tiered roles (`planner`, `reviewer`, `implementer`, `scout`, …) that dynamic workflows delegate to in parallel. Index + tiering: [`.agents/context/subagents.md`](.agents/context/subagents.md).
- **Autonomous loops** — bounded, review-gated loops (`/build-loop`, `/fix-loop`, `/review-loop`, …) with hard iteration caps. See [`.agents/workflows/autonomous-loops.md`](.agents/workflows/autonomous-loops.md).

Add new ones with `/create-command` / `/create-skill`; all surfaces are schema-checked by `verify-harness.sh`.

## Deeper Documentation

| Topic | File |
|---|---|
| Architecture | [`.agents/context/architecture.md`](.agents/context/architecture.md) |
| Conventions | [`.agents/context/conventions.md`](.agents/context/conventions.md) |
| Environment setup | [`.agents/context/environment.md`](.agents/context/environment.md) |
| Known issues | [`.agents/context/known-issues.md`](.agents/context/known-issues.md) |
| Review workflow | [`.agents/workflows/review.md`](.agents/workflows/review.md) |
| Testing workflow | [`.agents/workflows/testing.md`](.agents/workflows/testing.md) |
| Evaluator rubric | [`evaluator-rubric.md`](evaluator-rubric.md) |
| Agent teams | [`.agents/workflows/agent-teams.md`](.agents/workflows/agent-teams.md) |
| Dynamic workflows | [`.agents/workflows/dynamic-workflows.md`](.agents/workflows/dynamic-workflows.md) |
| Autonomous loops | [`.agents/workflows/autonomous-loops.md`](.agents/workflows/autonomous-loops.md) |
| Slash commands | [`.agents/context/slash-commands.md`](.agents/context/slash-commands.md) |
| Harness failure modes | [`.agents/context/failure-modes.md`](.agents/context/failure-modes.md) |
| Template adoption | [`.agents/workflows/adoption.md`](.agents/workflows/adoption.md) |
| Harness index | [`.agents/README.md`](.agents/README.md) |
| Runtime portability model | [`.agents/context/runtimes.md`](.agents/context/runtimes.md) |
